# Powerboard UI (prototype)

Entry point: `ui/admin.html`

Run (required for HTML includes via `fetch()`):

```powershell
cd d:\Freelancer\JOBS\@sertody\PowerDistributionBoard\ui
python -m http.server 8080
```

Open:

- `http://localhost:8080/admin.html`

